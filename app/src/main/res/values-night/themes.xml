<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme application theme using Material3 -->
    <style name="Theme.TodoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main primary color -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary color (Accent) -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.TodoApp.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />
    <style name="Theme.TodoApp.PopupOverlay" parent="ThemeOverlay.Material3.Dark" />
</resources>
